// @flow
import { StyleSheet } from 'react-native'

import { COLORS } from '../../themes'

const styles = {
  title: {
    color: COLORS.white,
    marginLeft: 5
  },
  subtitle: {
    marginLeft: 5
  }
}

export default StyleSheet.create(styles)
