import { StyleSheet } from 'react-native'

const styles = {
  container: {
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    bottom: 0
  }
}

export default StyleSheet.create(styles)
